services:
  app:
    build: .
    container_name: loan_manager_app
    ports:
      - "8080:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - APP_KEY=base64:vgOSb45OtAflvGCWJrIT7TTwOCAHlotU1G0swRV4i84=
      - APP_DEBUG=true
      - APP_ENV=local
      - SESSION_DRIVER=file
      - CACHE_STORE=array
      - LOG_CHANNEL=errorlog # Esto envía los errores a la consola de Docker, no a un archivo
      - DB_CONNECTION=mariadb
      - DB_HOST=host.docker.internal
      - DB_PORT=3306
      - DB_DATABASE=test
      - DB_USERNAME=jolman
      - DB_PASSWORD=Ianeph21
    volumes:
      - .:/var/www/html
      # Obligamos a Docker a usar carpetas internas para que no toque tu Arch
      - /var/www/html/storage
      - /var/www/html/bootstrap/cache
